# Changelog

本文档记录文档助手各版本的用户可见变更。  
发版时 GitHub Release 说明会自动截取对应版本章节。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

---

## [Unreleased]

---

## [0.1.7] - 2026-08-17

### 新增

- **三层格式策略（产品 A）**：明确 L1 `.mdoc` 权威精编、L2 项目原文件打开什么存什么、L3 导出交换可能有损
- **L2 统一 source 保真**：`.md` / `.txt` / `.html` / `.docx` 打开时记录原文件内容；未编辑保存一律原样写回，已编辑再走分格式最优策略
- **导出有损提示**：导出弹窗与帮助中心标明 HTML / Markdown / Word / PDF 为交换格式，复杂排版可能有损；长期精编请用 `.mdoc`
- **另存为 .mdoc**：顶栏主按钮「保存」+ **更多** 菜单「另存为 .mdoc」；另存后**自动切换**到新 `.mdoc` 继续精编
- **顶栏精简**：去掉并排双主次按钮与标题旁格式/脏状态标签，避免信息噪声
- **保存结果分类提示**：原样保存 / patch 保留样式 / 转换可能有损 / 已保存 .mdoc
- **快捷键**：`Ctrl/Cmd+S` 保存；`Ctrl/Cmd+Shift+S` 另存为 .mdoc

### 修复

- **文件模式预览表格不可见**：预览区补齐与编辑器一致的表格样式（边框、表头底色、斑马纹等），文件模式只读预览可正常显示表格
- **编辑器空段落 Placeholder**：空内容改为真正空段落，避免 TipTap 解析为 hardBreak 导致占位符不显示

### 样式

- **下拉/右键菜单统一**：编辑器工具栏下拉、斜杠菜单、项目/大纲「更多」等统一为共享 `ContextMenu` 样式（Regular 字重、悬停/按下态、长文案截断）
- **项目「更多」菜单去分隔线**：与大纲层级「更多」一致，打开项目位置与移除项目之间不再显示分割线
- **大纲「更多」加宽**：支持完整显示「导出HTML（含子文档）」等长菜单项
- **顶栏与项目首页**：按设计稿调整 Logo/版本、「发现新版本」标签、帮助与操作按钮图标与间距
- **编辑器工具栏与页内大纲**：工具栏高度与按钮 hover 态、右侧「在本页」大纲区样式对齐设计稿
- **弹窗与表单控件**：导出/新建/分享/颜色选择等弹窗按钮与输入框 focus 边框统一为设计规范色

### 重构

- **共享 ContextMenu 组件**：抽出 `ContextMenuPanel` / `ContextMenuItem`，供大纲、项目列表、侧栏与编辑器下拉复用
- **组件拆分与 UnoCSS**：主界面按 layout / sidebar / outline / file-tree / project 拆分；样式迁移至 UnoCSS，加快构建

### 文档

- README、使用手册、帮助弹窗同步三层格式说明与各格式保存行为表
- **UI 规范**：新增 `UI规范.md` 与 `AGENTS.md`，锁定色板、字号、弹窗/按钮/菜单约定，约束后续 AI 与人工改 UI

---

## [0.1.6] - 2026-08-13

### 新增

- **30 秒自动保存**：编辑中的当前文档会每 30 秒自动写入本地文档库或项目原文件，降低未手动保存时更新/关闭导致内容丢失的风险
- **关键操作前保存**：安装更新、关闭窗口选择最小化到托盘或退出应用前，会先强制保存当前文档
- **DOCX 原包保真保存**：导入 `.docx` 时保留原始 Office 包；未编辑直接保存会原样写回，编辑后优先 patch 原 `word/document.xml` 文本节点，尽量保留 Word/WPS 原样式、页边距、主题与字体资源

### 修复

- **切换/卸载前原文件被重写**：移除编辑器卸载时的无条件 HTML 回写，避免只是打开或切换页面就把 `.txt` / `.docx` 源文件规范化覆盖
- **安装更新被关闭确认打断**：点击「立即安装并重启」时会绕过关闭应用确认弹窗，避免打断安装/重启流程
- **TXT 空格与换行被重写**：`.txt` 导入后改用纯文本保真结构，写回时保留原始空格与换行风格；未编辑保存直接复用原始文本，避免文件大小因空白字符被折叠而变化
- **DOCX 保存后样式变化**：`.docx` 写回原文件时不再额外插入文件名标题，保留空段落，并增强 WPS/Word 标题样式映射

### 样式

- **自动保存状态**：底部保存状态文案改为「已自动保存」，更新时间精确到秒

---

## [0.1.5] - 2026-08-13

### 新增

- **项目首页帮助入口**：项目首页顶部新增「帮助」按钮，点击后与编辑页帮助按钮一致，可打开帮助弹窗并手动检查更新
- **启动自动检查更新**：应用启动进入后自动静默检查一次更新；无更新不提示，有更新时在首页标题旁显示「发现新版本!」标签
- **更新标签入口**：发现新版本后，可点击首页标题旁的绿色更新标签打开更新弹窗
- **关闭应用确认**：关闭窗口时增加应用内确认弹窗，可选择「最小化到托盘」或「退出应用」，并支持记住选择、下次不再询问

### 修复

- **启动更新事件丢失**：自动检查更新迁移到渲染端监听建立后触发，避免主进程过早发送更新事件导致弹窗或状态漏掉
- **首页帮助无反应**：修复项目首页分支未渲染帮助弹窗导致点击帮助按钮无响应的问题

### 样式

- **更新标签样式**：按设计稿调整为绿色标签样式，文案为「发现新版本!」

---

## [0.1.4] - 2026-08-13

### 新增

- **导入入口拆分**：导入弹窗底部新增独立「选择文件」与「选择文件夹」按钮，避免 Windows 文件/文件夹混合选择框兼容问题

### 修复

- **Windows 拖拽导入无反应**：拖拽导入改为通过 Electron 安全桥接获取真实文件路径，修复 Windows / 新版 Electron 下 `file.path` 为空导致导入无响应的问题
- **项目新建文件不可编辑**：新建 `.mdoc` 时自动创建默认大纲节点与空段落，并在创建后刷新文件树、选中新文件并进入可编辑大纲
- **项目文件打开不可编辑**：从项目文件树或单文件项目打开文件后直接进入大纲编辑模式，避免停留在只读预览/空态

### 样式

- **导入弹窗按钮布局**：移除拖拽区域内的选择按钮，底部统一显示「取消 / 选择文件 / 选择文件夹」
- **项目页文案**：搜索框占位文案调整为「搜索」；列表表头「项目名称」调整为「文件/文件夹名称」

---

## [0.1.3] - 2026-08-13

### 新增

- **复制层级链接**：大纲树层级更多菜单新增「复制层级链接」，可复制当前文档指定层级的内部跳转链接

### 修复

- **HTML 内部锚点跳转**：修复将其他层级的锚点/层级链接插入正文后，HTML 预览或分享页点击不会切换到目标层级的问题；外部网页链接仍按浏览器默认方式跳转
- **标题锚点失效**：修复 HTML 预览页生成“在本页”目录时重写标题 `id`，导致复制的标题锚点无法定位的问题

---

## [0.1.2] - 2026-08-12

### 新增

- **本地文件夹项目**：支持打开本地文件夹作为项目，左侧以文件树展示 `.mdoc` / `.md` / `.txt` / `.html` / `.htm` / `.docx` 文件，并可在文件树与大纲树之间切换
- **项目文件原路径保存**：从项目中打开 `.mdoc`、`.md`、`.html`、`.txt`、`.docx` 后，保存直接写回当前原文件，不再创建隐藏副本或读取默认库同名文档
- **Markdown 原文件编辑**：`.md` 文件进入应用后显示 Markdown 内容，保存时写回同一个 `.md` 文件，并增加空内容覆盖保护，避免已有 Markdown 被写成空文件
- **HTML 可逆导入**：导出/分享生成的 HTML 内嵌文档状态，再导入时可恢复大纲树与节点内容；普通 HTML 会尽量提取正文内容

### 修复

- **文件树重复渲染**：修复文件夹/文件在左侧树中重复出现的问题
- **项目文件打开预览**：修复项目文件树中点击文件后未完整进入可编辑大纲/预览流程的问题
- **HTML 分享内容为空或旧内容**：分享服务优先返回当前正在分享的 HTML，避免根路径访问到旧页面或空页面
- **HTML 写回格式**：项目中的 HTML 文件保存时写回完整预览页模板，而不是正文片段
- **Markdown 正文目录残留**：Markdown 中自动生成的“目录”锚点列表会作为空目录节点处理，避免在正文区显示一整块蓝色链接目录
- **分享按钮状态**：项目文件树模式下，未选中文件或选中的是文件夹时，分享按钮置灰并提示先选择文件
- **标题显示**：分享与导出 HTML 的页面标题、默认文件名只显示文件名，不显示路径和格式扩展名
- **Markdown 标题前内容丢失**：修复 `.md` 中第一个标题前的 frontmatter、前言或说明内容导入后丢失的问题
- **Markdown 目录章节误删**：不再自动清空标题为“目录 / TOC”的章节内容，避免真实目录说明被误删
- **文档预览空白**：修复项目文件在文档模式下只显示空态、看不到已打开文件内容的问题
- **右键菜单贴边裁切**：修复文件树右键菜单和项目更多菜单靠近屏幕边缘时被窗口边界裁掉的问题
- **同名版本混淆**：修复项目 `.mdoc` 与默认库同名 `.mdoc` 混用导致“已保存内容导入项目后看不到”的问题；项目文件现在只读写自身路径

### 样式

- **文件树图标**：文件夹图标替换为新版线性文件夹 SVG；文件图标替换为 `file-04` SVG
- **图标交互色**：文件夹与文件图标默认 `#8D8E99`，悬浮/选中为 `#131212`
- **树节点间距**：文件夹/文件图标与名称间距统一为 8px
- **大纲树显示**：大纲节点列表移除重复文件图标；大纲顶部文件名保留文件图标并与名称同色
- **文档模式空态**：未进入大纲编辑时提示调整为「切换到大纲模式下编辑文件内容」

### 文档

- 重写 README 项目介绍：补充应用定位、核心能力、项目文件原路径保存策略、支持格式、技术栈与发布流程
- 更新使用手册：补充本地文件夹项目、Markdown / `.mdoc` 原文件保存规则、HTML 往返导入导出、分享限制、右键菜单和数据恢复说明

---

## [0.1.1] - 2026-08-10

### 新增

- **大纲模式搜索**：大纲树支持按节点名过滤（大小写不敏感、保留匹配节点及其祖先链、结果全展开、无匹配显示提示、过滤时禁用拖拽）
- **回车快捷操作**：搜索框 Enter 后文档模式仅剩一个匹配项时自动选中；大纲模式跳转至第一个匹配节点

### 修复

- **搜索框输入被吞（macOS）**：移除输入框 200ms 防抖延迟，改为即时受控更新，避免父组件重渲染时输入内容被重置
- **回车无反应**：搜索框新增 Enter 处理（跳过输入法组合中状态）
- **文档列表搜索大小写不敏感**：匹配改为忽略大小写
- **顶栏按钮尺寸与对齐**：右上角 保存 / 导入 / 导出 / 分享 / 删除 / 帮助 按钮高度由 34px 修正为 32px，内容改为居中对齐（items-center）
- **线框按钮无交互反馈**：分享 / 删除 / 帮助 白底线框按钮补充悬浮（hover）与点击（active）背景反馈

### 样式

- HTML 预览 / 分享页布局细化：内容自适应宽度、标题/段落/表格/代码块间距与圆角调整
- 侧栏（大纲树 / 预览页）滚动条自动隐藏：不滚动时隐藏，滚动或悬停时显示细条

---

## [0.1.0] - 2026-08-10

### 新增

- 桌面端文档助手：大纲编辑、本地 `.mdoc`、局域网分享、导出 Word / PDF / Markdown
- 推送 `v*` tag 时经 GitHub Actions 构建 **Windows** 与 **macOS** 安装包并上传至 GitHub Releases
- 集成 **electron-updater**，对接 GitHub Releases
- 启动后静默检查更新；帮助页支持手动「检查更新」
- 更新弹窗：稍后再说 / 打开下载页 / 下载更新（含进度）
- Windows / macOS：下载后可「立即安装并重启」

### 修复

- **macOS 应用内更新**：下载完成后**原地覆盖**当前 `.app` 并自动重启，避免打开 dmg/zip 后出现两个应用并存
- 自动替换失败时回退为打开安装包，并提示选择「替换」而非「保留两者」
- 收紧引用块（blockquote）内外间距：编辑器、PDF 导出、**HTML 预览 / 分享页**一致
- 提高弹层（modal）z-index，避免被编辑器工具栏遮挡
- 斜杠菜单在触发字符删除后正确关闭
- Release 产物命名使用 ASCII `DocAssistant-${version}-${os}-${arch}`，避免 GitHub 上文件名乱码

### 文档

- 使用手册、应用内帮助与项目 README
- 说明 macOS / Windows 均可「立即安装并重启」，补充双应用处理 FAQ
- 增加 macOS「应用已损坏」隔离属性（quarantine）处理说明

### 安装包

- Windows：`DocAssistant-0.1.0-win-x64.exe`
- macOS：`DocAssistant-0.1.0-mac-arm64.dmg` / `.zip`

---

## 链接

- [Releases](https://github.com/wenlong301-hue/document-assistant/releases)
- [使用手册](./使用手册.md)
