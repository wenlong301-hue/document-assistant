# 文档助手 使用手册

**产品名称：** 文档助手  
**当前版本：** 0.1.1  
**原生文档格式：** `.mdoc`（文档助手文档）  
**适用平台：** macOS / Windows 桌面端（推荐），浏览器 Web 端（功能子集）

---

## 目录

1. [简介](#1-简介)
2. [安装与首次启动](#2-安装与首次启动)
3. [界面一览](#3-界面一览)
4. [快速开始](#4-快速开始)
5. [文档管理](#5-文档管理)
6. [大纲结构](#6-大纲结构)
7. [编写内容](#7-编写内容)
8. [局域网分享与手机预览](#8-局域网分享与手机预览)
9. [导出](#9-导出)
10. [快捷键](#10-快捷键)
11. [存储与备份](#11-存储与备份)
12. [桌面端与 Web 端差异](#12-桌面端与-web-端差异)
13. [常见问题](#13-常见问题)

---

## 1. 简介

文档助手是一款面向写作与知识整理的**多级文档工具**。你可以：

- 用**文档 + 大纲树**管理章节结构（类似一本书的目录）
- 在每一层节点中使用**富文本编辑器**写作
- 通过**局域网分享**让同事/手机浏览器只读预览
- 导出为 **HTML / Markdown / Word / PDF**

适合：产品说明、会议纪要、技术方案、操作手册、分层知识库等场景。

---

## 2. 安装与首次启动

### 2.1 下载安装包

从 GitHub Releases 下载对应系统安装包：

> https://github.com/wenlong301-hue/document-assistant/releases

| 系统 | 推荐文件 |
|------|----------|
| macOS（Apple 芯片） | `DocAssistant-x.x.x-mac-arm64.dmg` |
| Windows（64 位） | `DocAssistant-x.x.x-win-x64.exe` |

### 2.2 macOS 安装

1. 打开 `.dmg`，将「文档助手」拖入「应用程序」
2. 首次打开可能遇到未签名相关提示（见下方）。**不要**点「移到废纸篓」，应用本身通常并未损坏。

#### 情况 A：提示「无法验证开发者」/「无法打开，因为来自身份不明的开发者」

- 在应用图标上 **右键 → 打开 → 打开** 一次  
- 或打开 **系统设置 → 隐私与安全性**，找到相关提示后点 **仍要打开**

#### 情况 B：提示「“文档助手”已损坏，无法打开。你应该将它移到废纸篓。」

这是浏览器下载后的**隔离属性（quarantine）** + 未签名导致的误报，按下面处理：

1. 确认应用已在「应用程序」中（路径一般为 `/Applications/文档助手.app`）
2. 打开 **终端**，执行：

```bash
xattr -cr "/Applications/文档助手.app"
```

若仍在「下载」文件夹，把路径改成实际位置，例如：

```bash
xattr -cr ~/Downloads/文档助手.app
```

3. 再双击打开；若仍被拦，用 **右键 → 打开 → 打开**
4. 仍不行时可检查并再清一次：

```bash
xattr -l "/Applications/文档助手.app"
sudo xattr -cr "/Applications/文档助手.app"
```

### 2.3 Windows 安装

1. 运行 `.exe` 安装程序（NSIS）
2. 可自定义安装目录，并创建桌面/开始菜单快捷方式
3. 若出现 SmartScreen「未知发布者」提示，选择**仍要运行**（未签名构建的正常现象）

### 2.4 首次启动

- 默认窗口约 1400×900（最小约 900×600）
- 桌面端会自动读取本机文档目录中的 `.mdoc` 文件
- 生产版不显示系统菜单栏

### 2.5 Web 端（可选）

开发或内网部署时也可在浏览器中使用。Web 端可编辑与导出，但**不能提供真实局域网分享服务**（详见第 12 节）。

---

## 3. 界面一览

```
┌──────────────────────────────────────────────────────────┐
│ 文档助手          [保存] [导入] [导出] [分享] [删除]      │  ← 顶栏
├────────────┬─────────────────────────────────────────────┤
│ 搜索…      │  标题（可编辑）                             │
│ 文档 | 大纲│  ┌─────────────────────────────────────┐   │
│ 新建…      │  │ 富文本工具栏                         │   │
│ ─────────  │  │ 正文编辑区                           │   │
│ 文档列表   │  │                                     │   │
│ 或大纲树   │  └─────────────────────────────────────┘   │
│            │  保存状态 · 字数统计 · 大纲                 │
│ 分享状态   │                                             │
└────────────┴─────────────────────────────────────────────┘
```

| 区域 | 说明 |
|------|------|
| **顶栏** | 品牌名、保存、导入、导出、分享、删除 |
| **左侧栏** | 搜索、模式切换（文档 / 大纲）、新建、列表或大纲树、分享状态 |
| **右侧工作区** | 标题 + 编辑器（大纲模式）；文档模式显示引导空态 |
| **编辑器底栏** | 保存状态、字符/字数统计、页内大纲入口 |

---

## 4. 快速开始

按下面 5 步即可完成第一次写作：

1. **新建文档**  
   左侧点 **新建文档** → 输入名称 → **确定**
2. **进入大纲**  
   自动进入 **大纲** 模式，并带有一个与文档同名的根节点
3. **添加结构（可选）**  
   点 **新建文件** 或节点菜单 **添加子文档**，搭好章节树
4. **开始写作**  
   选中节点，在右侧编辑器输入内容；支持工具栏、斜杠命令 `/`、Markdown 快捷输入
5. **保存 / 分享 / 导出**  
   - 自动保存会在后台进行  
   - 顶栏 **保存** 可另存/下载 `.mdoc`  
   - **分享** 供局域网预览；**导出** 输出 HTML/Markdown/Word/PDF  

---

## 5. 文档管理

### 5.1 模式说明

| 模式 | 作用 |
|------|------|
| **文档** | 管理文档列表：新建、搜索、重命名、删除、导出 HTML |
| **大纲** | 管理当前文档的多级结构，并编辑各节点正文 |

> 提示：实际写作内容在 **大纲模式** 下完成。文档模式右侧若显示「该文档暂无内容，请在大纲模式下创建内容」，请切换到大纲。

### 5.2 新建文档

1. 在 **文档** 模式点 **新建文档**
2. 弹窗输入名称（不能为空）
3. 确定后进入大纲，并创建默认根节点

### 5.3 打开文档

在文档列表中点击文档名即可打开，并进入该文档的大纲视图。

桌面端启动时会加载默认目录下的全部 `.mdoc`：

- macOS / Windows：用户「文档」目录下的 **`DocAssistant`** 文件夹  
  例如：`~/Documents/DocAssistant/*.mdoc`

### 5.4 搜索文档

左侧搜索框占位为 **搜索文档...**，按**文档名称**包含匹配过滤列表。

> 大纲模式下的搜索框目前主要用于界面一致；请以文档模式搜索文档为准。

### 5.5 重命名

- **文档：** 列表右侧 **⋯** → **重命名**
- **大纲节点：** 节点 **⋯** → **重命名**，或直接编辑顶部标题  
  - 标题编辑：`Enter` 确认，`Esc` 取消

### 5.6 删除

入口：顶栏 **删除**，或列表/大纲菜单 **删除**。

确认弹窗标题为 **提示**，删除**不可撤销**，文案会区分：

- 删除整篇文档（含全部子内容）
- 删除分支节点（含子节点）
- 删除叶子节点内容

### 5.7 保存为 `.mdoc`

| 端 | 行为 |
|----|------|
| **桌面** | 弹出「选择文档保存位置」，写出 `{名称}.mdoc`，提示「已保存到选择的位置」 |
| **Web** | 浏览器下载 `{名称}.mdoc`，提示「已下载文档文件」 |

日常编辑时，桌面端会**自动防抖写入**本地库；Web 端写入浏览器 IndexedDB。

未选择文档时保存会提示：**请先新建或选择文档**。

### 5.8 导入

顶栏 **导入**，支持：

| 格式 | 说明 |
|------|------|
| **`.mdoc`** | 文档助手原生 JSON 结构（推荐往返备份） |
| **`.md`** | Markdown → 简易 HTML，首个标题可作为文档名 |
| **`.txt`** | 纯文本按段落导入 |
| **`.docx`** | Word 文档转为可编辑 HTML 内容 |

导入成功后进入大纲模式。失败可能提示 **导入失败** 或 **mdoc 文件格式错误**。

### 5.9 什么是 `.mdoc`

- 扩展名：`.mdoc`
- 本质：JSON 文件
- 主要内容：文档名、大纲树、各节点 HTML 正文、更新时间
- 安装后可在系统中关联为「**文档助手文档**」（视安装器与系统策略而定）

---

## 6. 大纲结构

大纲把一篇文档拆成**多级节点**，每个节点有独立正文，适合长文分层。

### 6.1 切换到大纲

左侧点 **大纲**。若尚未选中节点，右侧可能显示「当前文档未编辑内容」。

### 6.2 节点操作

| 操作 | 入口 / 说明 |
|------|-------------|
| **新建文件** | 新建根级节点 |
| **添加子文档** | 节点菜单；默认名「新建子文档」 |
| **重命名** | 节点菜单或顶部标题 |
| **克隆** | 节点菜单；名称追加 ` (副本)` |
| **删除** | 节点菜单或顶栏删除 |
| **展开 / 收起** | 节点前箭头 |
| **拖拽排序** | 拖动手柄调整顺序与父子关系 |
| **预览时隐藏本层** | 分享/导出 HTML 中不展示该层内容 |
| **预览时显示本层** | 恢复参与预览 |
| **导出HTML（含子文档）** | 导出该节点及其子树为预览 HTML |

### 6.3 层级与拖拽限制

- 大纲层级约 **最多 6 层**（过深时无法再添加子文档）
- **根节点不能拖到非根节点之下**（保持文档根结构清晰）
- 导出时标题级别会映射到 HTML 的 h1–h6

### 6.4 预览收录规则

节点默认会参与分享/导出预览。若：

- 在菜单中选择了 **预览时隐藏本层**，或  
- 节点正文为空  

则该节点通常**不会**作为可预览章节出现。点击空节点时，预览可能落到子树中第一个有内容的节点。

### 6.5 空态提示

| 场景 | 提示 |
|------|------|
| 没有任何文档 | 暂无文档，请新建文档 |
| 文档模式但未在大纲写内容 | 该文档暂无内容，请在大纲模式下创建内容 |
| 大纲未选中可编辑内容 | 当前文档未编辑内容 |

---

## 7. 编写内容

仅在 **大纲模式** 且已选中节点时，右侧富文本编辑器可用。

占位提示：**输入 / 呼出命令，或直接开始写作**

### 7.1 工具栏功能

| 类别 | 能力 |
|------|------|
| 段落 | 正文、一至五级标题 |
| 字体 | 系统默认、PingFang SC、微软雅黑、宋体、楷体等 |
| 字号 | 约 12px–32px |
| 样式 | 加粗、斜体、删除线、下划线 |
| 颜色 | 字体颜色、背景高亮（含自定义色值） |
| 列表 | 任务列表、有序列表、无序列表 |
| 缩进 | 增加 / 减少缩进 |
| 对齐 | 左、居中、右、两端对齐 |
| 块级 | 引用块、代码块 |
| 链接 | 插入链接、清除链接 |
| 媒体 | 图片、视频、附件 |
| 其它 | 表格、清除格式、复制锚点链接 |

### 7.2 斜杠命令 `/`

在**空行行首**输入 `/`，可快速插入：

一级标题、二级标题、三级标题、正文、有序/无序/任务列表、引用块、代码块、图片、视频、附件、表格  

导航：`↑` `↓` 选择，`Enter` 确认，`Esc` 关闭。

### 7.3 Markdown 快捷输入

在行首输入下列标记后按**空格**可转换：

| 输入 | 结果 |
|------|------|
| `#` ~ `######` | 一至六级标题 |
| `>` | 引用 |
| `1.` | 有序列表 |
| `-` / `*` / `+` | 无序列表 |
| `` ``` `` | 代码块 |

### 7.4 链接

- 快捷键：`Ctrl/Cmd + K`
- 弹窗可填 **显示文本** 与 **链接地址**
- 支持 `http(s)://`、`mailto:`、页内 `#` 等；未写协议时可能自动补全 `https://`
- 可用 **清除链接** 去掉链接样式

### 7.5 表格

1. 工具栏或斜杠命令插入表格  
2. 弹窗设置 **行数 1–20**、**列数 1–10**  
3. 选中表格后可用浮动条：  
   **上方行 / 下方行 / 删行 · 左列 / 右列 / 删列 · 删表格**  
4. 支持拖拽调整行高  

### 7.6 图片

- 支持：PNG、JPEG、WebP、GIF  
- **不支持 SVG**  
- 源文件建议不超过 **20MB**；嵌入前会压缩，嵌入体积约 **4MB** 上限（GIF 更严格）  
- 支持粘贴、拖放  
- 选中图片可调宽度（25% / 50% / 70% / 100% / 原图）及拖角缩放  

### 7.7 视频

- 常见 `video/*` 格式  
- 单文件上限约 **20MB**  
- **导出 PDF 时视频不会输出**（打印场景隐藏）

### 7.8 附件

- 单文件上限约 **10MB**  
- 以可点击附件块形式插入正文  

### 7.9 代码块

- 工具栏或 `` ``` `` 快捷创建  
- 退出：`Esc` 或 `Ctrl/Cmd + Enter`  
- 分享预览页支持 **复制** 代码  

### 7.10 锚点链接

工具栏 **复制锚点链接** 可生成定位到当前文档/节点的链接（含查询参数），便于分享给已打开应用的同事跳转。

常见提示：

- 成功：锚点链接已复制到剪贴板  
- 失败：剪贴板不可用、光标位置不合适、目标内容不存在等  

### 7.11 页内大纲与字数

- 编辑器侧可显示 **大纲**（由正文标题生成）；无标题时显示「暂无标题」  
- 底栏显示：`{n}字符 {m}字`  
- 保存状态：**保存中...** / **已保存，更新于 …** / **未保存**  

### 7.12 自动保存

编辑内容会自动保存到本地存储（桌面写文件，Web 写 IndexedDB）。  
`Ctrl/Cmd + S` 会刷新保存状态并提示「已保存」。

---

## 8. 局域网分享与手机预览

### 8.1 适用场景

把当前文档以**只读网页**形式分享给同一 Wi-Fi / 局域网内的同事，或用手机浏览器预览多级目录与正文。

### 8.2 桌面端开启分享

1. 先选中要分享的文档  
2. 点顶栏 **分享**  
3. 在弹窗中打开 **开启分享**  
4. 复制 **访问链接**（形如 `http://192.168.x.x:6535`）发给对方  

说明：

- 端口固定为 **6535**  
- **电脑与文档助手需保持开启**，关闭分享或退出应用后链接失效  
- 侧栏底会显示 **分享中** / **未开启分享**  
- 若端口被占用：提示「端口 6535 已被占用，请关闭占用程序后重试。」

### 8.3 预览页结构

| 端 | 体验 |
|----|------|
| **电脑浏览器** | 左：文档大纲树 · 中：正文 · 右：「在本页」标题目录 |
| **手机浏览器** | 顶栏显示当前标题；左侧抽屉打开多级目录；右侧抽屉打开「在本页」 |

- 仅展示**有内容且未隐藏**的节点  
- 大纲可展开/收起，点击切换章节  
- 代码块可 **复制**  

### 8.4 Web 端分享差异

浏览器版**不能**在局域网起 HTTP 服务，只能：

- 生成本机预览页（blob 链接）  
- **下载 HTML** 后自行分发  

真·局域网访问请使用**桌面应用**。

### 8.5 分享相关提示

| 提示 | 含义 |
|------|------|
| 分享已开启 / 已关闭 | 服务状态变化 |
| 请先选择文档 | 未选中文档 |
| 分享开启失败 | 其它错误（含端口占用） |
| 预览加载异常 | 请重新导出或重新开启分享 |

---

## 9. 导出

### 9.1 导出文档弹窗

顶栏 **导出** 打开 **导出文档**：

**导出范围**

| 选项 | 说明 |
|------|------|
| **当前页** | 当前大纲节点及其子集 |
| **整个文档** | 当前文档全部可导出内容 |

**导出格式**

| 格式 | 适合场景 |
|------|----------|
| **HTML** | 预览与离线分享（含大纲导航的完整预览页） |
| **Markdown** | 二次编辑；桌面端大图可拆到资源目录 |
| **Word** | `.docx`，便于 Word 继续排版 |
| **PDF** | 正式分发、打印归档 |

按钮：**取消** / **导出**（进行中显示 **导出中...**）

### 9.2 各端行为差异

| 格式 | 桌面端 | Web 端 |
|------|--------|--------|
| HTML | 系统另存为 | 浏览器下载 |
| Markdown | 另存为；图片可落盘到资源文件夹 | 下载 `.md`（图片可能内联较大） |
| Word | 生成 `.docx` 另存为 | 浏览器下载 `.docx` |
| PDF | 后台渲染为 PDF 文件 | 打开打印窗口，请选择「存储为 PDF」 |

### 9.3 快捷导出 HTML

- 文档列表 **⋯** → **导出HTML**  
- 大纲节点 **⋯** → **导出HTML（含子文档）**  

生成的 HTML 与分享预览页类似，支持多级目录与手机顶栏导航。

### 9.4 导出注意

- PDF **不包含视频**  
- 图片过多/过大时导出与存储都会变慢  
- 仅「可预览」节点会进入 HTML 预览结构  

成功提示 **导出成功**；取消为 **已取消导出**；失败为 **导出失败**。

---

## 10. 快捷键

| 快捷键 | 作用 |
|--------|------|
| `Ctrl/Cmd + S` | 保存状态刷新，提示已保存 |
| `Ctrl/Cmd + K` | 插入 / 编辑链接 |
| `/`（空行行首） | 打开斜杠命令菜单 |
| `↑` `↓` `Enter` `Esc` | 斜杠菜单导航 |
| Markdown 行首 + 空格 | 标题 / 列表 / 引用 / 代码块 |
| `Esc` 或 `Ctrl/Cmd + Enter` | 退出代码块 |
| 弹窗中 `Enter` / `Esc` | 确认 / 关闭 |
| 标题编辑 `Enter` / `Esc` | 提交 / 取消 |

> 生产版桌面端无系统菜单栏快捷键列表，以上为编辑器内快捷键。

---

## 11. 存储与备份

### 11.1 桌面端

| 项目 | 说明 |
|------|------|
| 默认目录 | `文档/DocAssistant/` |
| 文件格式 | `*.mdoc` |
| 建议备份 | 定期复制整个 `DocAssistant` 文件夹，或使用顶栏 **保存** / **导出** |

### 11.2 Web 端

| 项目 | 说明 |
|------|------|
| 主存储 | 浏览器 IndexedDB |
| 过大失败 | 可能提示「文档内容过大，浏览器存储失败，请导出文档后减少媒体文件」 |
| 建议 | 重要内容及时 **导出 .mdoc / HTML**，勿仅依赖浏览器缓存 |

### 11.3 体积建议

媒体以内嵌方式保存，会导致 `.mdoc` 迅速变大：

- 图片：控制源文件体积，优先压缩后插入  
- 视频：尽量 ≤ 20MB，正式归档用外链或单独文件  
- 附件：≤ 10MB  

---

## 12. 桌面端与 Web 端差异

| 能力 | 桌面（Electron） | Web 浏览器 |
|------|------------------|------------|
| 富文本编辑 / 大纲 | ✅ | ✅ |
| 本地 `.mdoc` 库目录 | ✅ `DocAssistant` | ❌（IndexedDB） |
| 局域网 HTTP 分享 | ✅ 端口 6535 | ❌（仅本机预览/下载 HTML） |
| 导出 Word / PDF / MD | 系统另存为 | 浏览器下载；PDF 走打印 |
| 顶栏「保存」 | 选文件夹另存 | 下载文件 |
| 文件关联双击打开 | 视系统而定 | 无 |

---

## 13. 常见问题

### Q1：macOS 提示无法打开 / 无法验证开发者？

当前发布包为**未签名**构建。请使用「右键 → 打开」，或在「隐私与安全性」中允许。详见 [2.2 情况 A](#22-macos-安装)。

### Q1b：macOS 提示「已损坏，无法打开 / 移到废纸篓」？

**不是真的损坏。** 从 Edge/Safari/Chrome 下载的未签名应用会被打上 `com.apple.quarantine`，系统会误报。

1. **不要**点「移到废纸篓」
2. 在终端执行（应用在「应用程序」时）：

```bash
xattr -cr "/Applications/文档助手.app"
```

3. 再打开；必要时 **右键 → 打开**，或：

```bash
sudo xattr -cr "/Applications/文档助手.app"
```

完整步骤见 [2.2 情况 B](#22-macos-安装)。

### Q2：Windows SmartScreen 拦截？

同样因未签名。选择更多信息 → 仍要运行即可。

### Q3：分享链接手机打不开？

请检查：

1. 是否使用**桌面端**开启了分享（Web 端不能局域网分享）  
2. 手机与电脑是否同一 Wi-Fi  
3. 电脑防火墙是否放行 **6535** 端口  
4. 应用与电脑是否仍在运行  
5. 链接是否为电脑局域网 IP（不是 `127.0.0.1`）

### Q4：提示端口 6535 被占用？

关闭占用该端口的其它程序，或重启电脑后再试。

### Q5：分享/导出 HTML 里看不到某章节？

可能原因：

- 该节点正文为空  
- 设置了 **预览时隐藏本层**  
- 需要重新开启分享或重新导出（旧链接仍是旧页面）

### Q6：手机上如何切换多级目录？

预览页顶部左侧打开**目录抽屉**，可展开多级大纲；右侧为「在本页」标题导航。

### Q7：浏览器提示存储失败？

媒体过大导致 IndexedDB/localStorage 超限。请减少图片/视频，或改用桌面端，并导出备份。

### Q8：PDF 里没有视频？

设计如此：PDF 打印链路会隐藏视频。请使用 HTML 分享或单独分发视频文件。

### Q9：不能插入 SVG？

暂不支持 SVG 图片，请转为 PNG / JPEG / WebP。

### Q10：导入 docx/md 格式错乱？

导入为「尽量保留」的转换，复杂排版（复杂表格、域代码、浮动图等）可能简化。重要文档建议导入后人工校对，或使用 `.mdoc` 往返。

### Q11：大纲最多几层？

约 **6 层**。更深层级请拆成多篇文档或合并章节。

### Q12：如何备份全部资料？

复制 `Documents/DocAssistant` 整个文件夹，或逐个 **保存 / 导出** `.mdoc` 与 HTML。

---

## 附录 A：界面文案速查

**顶栏：** 文档助手 · 保存 · 导入 · 导出 · 分享 · 删除  

**侧栏：** 搜索文档… / 搜索大纲… · 文档 / 大纲 · 新建文档 / 新建文件 · 默认文件夹 · 未开启分享 / 分享中  

**文档菜单：** 重命名 · 导出HTML · 删除  

**大纲菜单：** 添加子文档 · 重命名 · 预览时隐藏本层 / 预览时显示本层 · 导出HTML（含子文档） · 克隆 · 删除  

**分享：** 分享文档 · 访问链接 · 复制链接 · 已复制 · 下载HTML · 开启分享  

**导出：** 导出文档 · 当前页 · 整个文档 · HTML · Markdown · Word · PDF  

**通用：** 取消 · 确定 · 提示  

---

## 附录 B：版本与支持

- 版本号以应用内 / GitHub Release 标签为准（如 `v0.1.1`）  
- 问题反馈与更新：  
  https://github.com/wenlong301-hue/document-assistant/issues  
  https://github.com/wenlong301-hue/document-assistant/releases  

---

*本手册基于文档助手 0.1.x 功能整理。若界面与手册不一致，以实际软件为准。*
